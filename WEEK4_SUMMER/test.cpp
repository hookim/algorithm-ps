#include <iostream>
#include <vector>

using namespace std;
int main(){
    // int N;
    // vector <int> v; 
    // v.push_back(1);
    // v.push_back(99);

    // swap(v[0], v[1]);
    // cout << v[0];
    
    cout << -5 / 3 << endl;

    return 0;
}